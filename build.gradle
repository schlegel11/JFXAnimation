subprojects {
    apply plugin: 'java'
    apply from: "$rootProject.projectDir/gitCommands.gradle"

    version '2.0.0'

    repositories {
        jcenter()
        mavenCentral()
    }

    sourceCompatibility = 1.8
    targetCompatibility = 1.8
}